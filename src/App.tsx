import { useMemo, useState } from 'react'
import RT11Wallet from './RT11Wallet'
import RT11Money from './RT11Money'

type Agent = { name:string; role:string; E:number; I:number; C:number; D:number; impact:number; entropy:number }
type Tab = 'home'|'earn'|'good'|'how'|'economics'|'math'|'api'|'wallet'|'proof'|'safety'|'docs'
type DocExample = 'teacher'|'mediator'|'spammer'

const agents: Agent[] = [
  { name:'MediatorNode', role:'Calms a conflict', E:5.5, I:.93, C:.92, D:.30, impact:1.7, entropy:.45 },
  { name:'SafetyNode', role:'Checks AI output for risk', E:7.5, I:.90, C:.86, D:.50, impact:2.0, entropy:.65 },
  { name:'TeacherNode', role:'Makes safe learning material', E:7.0, I:.92, C:.88, D:.45, impact:1.4, entropy:.60 },
  { name:'BuilderNode', role:'Builds a useful public tool', E:8.0, I:.82, C:.80, D:.70, impact:1.8, entropy:.80 },
  { name:'SpamNode', role:'Uses AI to spam or mislead', E:8.5, I:.35, C:.30, D:2.50, impact:.55, entropy:2.20 },
]
const uap = (a:Agent) => (a.E*a.I*a.C)/Math.max(a.D,.0001)
const resonance = (a:Agent) => ((a.E*a.I*a.C)*a.impact)/Math.max(a.D*a.entropy,.0001)
const clamp = (n:number,min:number,max:number)=>Math.max(min,Math.min(max,n))

function Home({ setTab }:{setTab:(t:Tab)=>void}) { return <section className="sim-layout"><div className="rt-card hero-card elite-hero" id="rt11-landing"><div className="system-pill">FOR PEOPLE WHO WANT TO EARN WITH AI SAFELY</div><p className="eyebrow">ETHIC VAULT / RT11</p><h1>Use AI to do useful work. Get a higher payout score.</h1><p className="lead">RT11 is a prototype for paying people based on the value of their AI-assisted work. If your AI work helps people, reduces risk, teaches, builds, or improves a community, it earns more weight. If it spams, scams, manipulates, or spreads misinformation, it earns less.</p><div className="action-row"><button className="primary" onClick={()=>setTab('earn')}>How do I earn?</button><button onClick={()=>setTab('economics')}>Show the money model</button></div><div className="proof-row"><span>Earn safely</span><span>Help people</span><span>Avoid spam</span><span>Show proof</span></div></div><div className="rt-card"><div className="system-pill">START HERE</div><p className="eyebrow">THE SIMPLE VERSION</p><h2>RT11 is like a rewards program for good AI work.</h2><p>Imagine points, but for useful AI contribution. A helpful tutor bot, a clean research summary, a safer workflow, or a conflict-resolution tool can earn more score. Spam, fake content, fraud, or harassment earns less.</p></div><div className="rt-card"><div className="system-pill">WHO THIS IS FOR</div><p className="eyebrow">TARGET USERS</p><h2>Different people care about different parts.</h2><div className="kpi-grid"><div><span>Earn with AI</span><b>Money path</b><p>See what behavior could increase payout weight.</p></div><div><span>Social good</span><b>Impact path</b><p>See how helpful work and harm reduction are rewarded.</p></div><div><span>Builders</span><b>System path</b><p>See API, wallet, proof, and blockchain flow.</p></div><div><span>Math people</span><b>Model path</b><p>See UAP, Resonance, and treasury formulas.</p></div></div></div><div className="rt-card"><div className="system-pill">WHAT HAPPENS NEXT</div><p className="eyebrow">USER JOURNEY</p><h2>Understand it, test it, inspect it, then prove it.</h2><div className="checklist"><span>1. Read the earn path.</span><span>2. Try the payout demo.</span><span>3. Inspect the API JSON.</span><span>4. Deploy testnet contracts for live payout proof.</span></div></div></section> }
function Earn({ setTab }:{setTab:(t:Tab)=>void}) { return <section className="sim-layout"><div className="rt-card hero-card"><div className="system-pill">EARN SAFELY</div><p className="eyebrow">FOR AI USERS</p><h1>What kind of AI work could earn?</h1><p className="lead">Work that saves time is good. Work that helps people and lowers risk is better. RT11 rewards useful output, checked output, and work that other people can trust.</p><div className="action-row"><button className="primary" onClick={()=>setTab('demo')}>Try payout demo</button><button onClick={()=>setTab('wallet')}>Connect wallet</button></div></div><div className="rt-card"><div className="system-pill">GOOD AI WORK</div><p className="eyebrow">EXAMPLES</p><h2>Things that should earn more score.</h2><div className="checklist"><span>Creating accurate study guides and checking them before sharing.</span><span>Summarizing complex information clearly with sources.</span><span>Building tools that help a nonprofit, school, or small business.</span><span>Using AI to reduce conflict, confusion, or risk.</span></div></div><div className="rt-card"><div className="system-pill">BAD AI WORK</div><p className="eyebrow">WHAT LOSES WEIGHT</p><h2>Things that should earn less or get blocked.</h2><div className="checklist"><span>Spam, fake posts, bot flooding, or engagement farming.</span><span>Manipulation, scams, fraud, or fake authority.</span><span>Unchecked medical, legal, financial, or safety advice.</span><span>Harassment, impersonation, misinformation, or abuse.</span></div></div><div className="rt-card"><div className="system-pill">PAYOUT IDEA</div><p className="eyebrow">HOW MONEY COULD FLOW</p><h2>Everyone can get a base. Better work can earn a bonus.</h2><p>The UBI pool can include a baseline amount so people are not forced to compete for survival. The bonus pool is where useful, low-risk, high-impact work earns more.</p></div></section>}
function Demo() { const [treasury,setTreasury]=useState(1000); const [selected,setSelected]=useState(agents[0]); const rows=useMemo(()=>{ const scored=agents.map(a=>{ const R=resonance(a); const penalty=Math.max(0,a.D*(1-a.C)); const weight=clamp(1+Math.log1p(R)-.15*penalty,.25,10); return {...a,R,U:uap(a),weight,penalty} }); const total=scored.reduce((s,a)=>s+a.weight,0); const pool=treasury*.8; const base=(pool*.5)/scored.length; return scored.map(a=>({...a,payout:base+(pool*.5)*(a.weight/total)})) },[treasury]); const active=rows.find(r=>r.name===selected.name)||rows[0]; return <section className="sim-layout elite-sim" id="rt11-simulator"><div className="rt-card"><div className="system-pill">TRY IT</div><p className="eyebrow">PAYOUT DEMO</p><h2>Move the treasury slider. Pick a worker. See who gets more.</h2><label className="range"><span>Treasury Amount <b>{treasury}</b></span><input type="range" min="100" max="10000" step="100" value={treasury} onChange={e=>setTreasury(Number(e.target.value))}/></label><div className="kpi-grid"><div><span>Reward Pool</span><b>{Math.round(treasury*.8)}</b></div><div><span>System / Safety Pool</span><b>{Math.round(treasury*.2)}</b></div></div></div><div className="rt-card"><div className="system-pill">AI WORK TYPES</div><p className="eyebrow">CHOOSE ONE</p><div className="agent-list">{rows.map(r=><button key={r.name} className={selected.name===r.name?'active':''} onClick={()=>setSelected(r)}><strong>{r.name}</strong><small>{r.role}</small><span>Score {r.R.toFixed(1)} · Preview payout {r.payout.toFixed(1)}</span></button>)}</div></div><div className="rt-card"><div className="system-pill">WHY THIS RESULT</div><p className="eyebrow">PLAIN EXPLANATION</p><h2>{active.name}</h2><p>{active.role}</p><div className="kpi-grid"><div><span>Usefulness score</span><b>{active.R.toFixed(2)}</b></div><div><span>Risk / drift</span><b>{active.D}</b></div><div><span>Preview payout</span><b>{active.payout.toFixed(2)}</b></div><div><span>Result</span><b>{active.D > 1 ? 'Reduced' : 'Rewarded'}</b></div></div><p>Helpful work with low risk earns more. Harmful work with high risk earns less, even if it took a lot of effort.</p></div></section> }
function Economics(){ return <section className="sim-layout"><div className="rt-card hero-card"><div className="system-pill">ADVANCED ECONOMICS</div><p className="eyebrow">FOR PEOPLE WHO WANT THE MODEL</p><h1>Two loops: one measures work, one moves money.</h1><p className="lead">RT11 separates scoring from payment. First it estimates whether AI work created value. Then the treasury uses that score to split a baseline pool and a bonus pool.</p></div><div className="rt-card"><div className="system-pill">LOOP 1</div><p className="eyebrow">CONTRIBUTION LOOP</p><h2>AI work becomes reputation weight.</h2><div className="checklist"><span>1. A person uses AI to complete useful work.</span><span>2. The work is checked for effort, purpose, quality, and risk.</span><span>3. The score becomes a reputation weight.</span><span>4. Better reputation weight can increase payout share.</span></div></div><div className="rt-card"><div className="system-pill">LOOP 2</div><p className="eyebrow">TREASURY LOOP</p><h2>Reputation weight guides rewards.</h2><div className="checklist"><span>1. Money enters the treasury.</span><span>2. 80% goes toward UBI, public-good rewards, and contributors.</span><span>3. 20% supports operations, safety, governance, and infrastructure.</span><span>4. Payouts can be recorded on-chain as proof.</span></div></div><div className="rt-card"><div className="system-pill">UBI SYSTEM</div><p className="eyebrow">BASELINE + BONUS</p><h2>Base support plus usefulness bonus.</h2><p>The baseline pool helps verified participants. The bonus pool rewards useful work. That keeps the system from becoming pure competition while still encouraging high-value contribution.</p><div className="kpi-grid"><div><span>Baseline</span><b>Floor</b><p>Basic support for participation.</p></div><div><span>Bonus</span><b>Weight</b><p>Extra reward for usefulness and low risk.</p></div></div></div><div className="rt-card"><div className="system-pill">BLOCKCHAIN ROLE</div><p className="eyebrow">DUAL LOOP PROOF</p><h2>The blockchain is the receipt system.</h2><p>Scoring can happen off-chain through APIs and review. Payouts can happen on-chain so the treasury, wallet, transaction hash, and payout event are public and auditable.</p></div></section>}
function MathModel(){ return <section className="sim-layout"><div className="rt-card hero-card"><div className="system-pill">MATH MODEL</div><p className="eyebrow">FOR DETAIL PEOPLE</p><h1>The score rewards usefulness and penalizes risk.</h1><p className="lead">The math is intentionally simple for the prototype: useful effort goes up when purpose and quality are strong; value goes down when risk, spam, or confusion increase.</p></div><div className="rt-card"><div className="system-pill">UAP</div><p className="eyebrow">CORE SCORE</p><h2>UAP = (E × I × C) / D</h2><div className="checklist"><span>E = Energy / useful work effort</span><span>I = Intent / good purpose</span><span>C = Control / quality and checking</span><span>D = Drift / risk, harm, spam, confusion</span></div></div><div className="rt-card"><div className="system-pill">RESONANCE</div><p className="eyebrow">IMPACT SCORE</p><h2>R = ((E × I × C) × Impact) / (Drift × Entropy)</h2><p>This adds downstream impact. Teaching ten people safely should score better than generating ten useless posts. Harmful automation gets divided down by drift and entropy.</p></div></section>}
function ChainStatus(){ return <section className="rt-card"><div className="system-pill">PROOF</div><p className="eyebrow">CHAIN STATUS</p><h2>Blockchain proof is the next milestone.</h2><div className="status-list"><p><b>Treasury:</b> 0x27f780E6d46dF69347f954674bbDF39924e3D644</p><p><b>Anchor:</b> 0x9971c17e2d9638cbb63a2d2670db69fa4f335a8a98b222777a3838acf0b2b3e8</p><p><b>Status:</b> Testnet deployment pending</p></div></section> }
function Safety(){ return <section className="rt-card"><div className="system-pill">SAFETY FIRST</div><p className="eyebrow">WHY PAYOUTS ARE LOCKED</p><h2>Real payouts stay locked until testnet proves the system works.</h2><div className="checklist"><span>✓ Dry-run first</span><span>✓ Testnet before mainnet</span><span>✓ Wallet signature required</span><span>✓ Recipient list must be approved</span><span>□ Real payout after proof</span></div></section> }
function ApiTest(){ const [payload,setPayload]=useState({energy:7,intent:.9,control:.85,drift:.5,impact:1.5,entropy:.6}); const [status,setStatus]=useState('idle'); const [json,setJson]=useState<any>(null); async function run(){ setStatus('loading'); try{ const res=await fetch('/api/rt11/score',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)}); const data=await res.json(); setJson({status:res.status,ok:res.ok,data}); setStatus(res.ok?'success':'error'); }catch(e:any){ setJson({error:String(e?.message||e)}); setStatus('error'); }} const set=(k:keyof typeof payload,v:number)=>setPayload(p=>({...p,[k]:v})); return <section className="sim-layout"><div className="rt-card hero-card"><div className="system-pill">LIVE API TEST</div><p className="eyebrow">/api/rt11/score</p><h1>See the JSON returned by the scoring API.</h1><p className="lead">This panel calls the live API and prints the exact response.</p><div className="action-row"><button className="primary" onClick={run}>{status==='loading'?'Calling API…':'Run API Test'}</button><a className="button" href="/api/rt11/score" target="_blank" rel="noreferrer">Open GET Endpoint</a></div></div><div className="rt-card"><div className="system-pill">INPUTS</div>{Object.entries(payload).map(([k,v])=><label className="range" key={k}><span>{k} <b>{v}</b></span><input type="range" min="0" max={k==='energy'?10:k==='drift'?3:2} step="0.05" value={v} onChange={e=>set(k as keyof typeof payload,Number(e.target.value))}/></label>)}</div><div className="rt-card"><div className="system-pill">RESPONSE</div><p className="eyebrow">STATUS: {status.toUpperCase()}</p><pre>{json?JSON.stringify(json,null,2):'Click Run API Test to see live JSON here.'}</pre></div></section>}
function Docs(){ return <section className="sim-layout"><div className="rt-card"><div className="system-pill">DOCS</div><p className="eyebrow">WHO SHOULD READ WHAT</p><h2>Pick your path.</h2><div className="checklist"><span>Want to earn with AI? Read Earn and Demo.</span><span>Care about social good? Read Social Good and Safety.</span><span>Want the money model? Read Economics and Payouts.</span><span>Want proof? Read API Test and Proof.</span></div></div></section> }
function SocialGood(){ return <section className="sim-layout"><div className="rt-card hero-card"><div className="system-pill">SOCIAL GOOD</div><p className="eyebrow">WHY THIS MATTERS</p><h1>Pay people for lowering harm before it becomes damage.</h1><p className="lead">Good AI work can reduce confusion, teach skills, support care work, help communities, and make institutions more trustworthy. RT11 tries to make that work visible and rewardable.</p></div><div className="rt-card"><div className="system-pill">WHAT COUNTS</div><p className="eyebrow">PUBLIC GOOD EXAMPLES</p><div className="checklist"><span>Helping students learn safely.</span><span>Making public information easier to understand.</span><span>Reducing conflict and improving communication.</span><span>Building tools that help small groups do more with less.</span></div></div></section>}
export default function App(){ const [tab,setTab]=useState<Tab>('home'); const items:{id:Tab;label:string}[]=[{id:'home',label:'HOME'},{id:'earn',label:'EARN'},{id:'demo',label:'DEMO'},{id:'payouts',label:'PAYOUTS'},{id:'economics',label:'ECON'},{id:'math',label:'MATH'},{id:'api',label:'API'},{id:'wallet',label:'WALLET'},{id:'proof',label:'PROOF'},{id:'safety',label:'SAFETY'},{id:'good',label:'GOOD'},{id:'docs',label:'DOCS'}]; return <main className="app-shell lcars-shell"><header className="topbar lcars-top"><div><strong>RT11</strong><span>Safe AI Income System</span></div><a href="https://github.com/TheBluCog/ReactorTheory" target="_blank" rel="noreferrer">GitHub</a></header><div className="lcars-layout"><nav className="tabs lcars-nav">{items.map(t=><button key={t.id} className={tab===t.id?'active':''} onClick={()=>setTab(t.id)}>{t.label}</button>)}</nav><section className="app-content cinematic-tab lcars-content">{tab==='home'&&<Home setTab={setTab}/>} {tab==='earn'&&<Earn setTab={setTab}/>} {tab==='demo'&&<Demo/>} {tab==='payouts'&&<RT11Money/>} {tab==='economics'&&<Economics/>} {tab==='math'&&<MathModel/>} {tab==='api'&&<ApiTest/>} {tab==='wallet'&&<RT11Wallet/>} {tab==='proof'&&<ChainStatus/>} {tab==='safety'&&<Safety/>} {tab==='good'&&<SocialGood/>} {tab==='docs'&&<Docs/>}</section></div></main> }
